import React from "react";
import { render } from "react-dom";
import { Lilas } from "./Lilas.tsx";

const container = document.getElementById("app");

const element = <Lilas />;

render(element, container);
